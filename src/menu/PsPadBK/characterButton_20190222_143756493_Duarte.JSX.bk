import React from "react";

const CharacterButton = props => {
  const { personagem, player } = props;
  const color = (player || { color: "grey" }).color;
  return (
    <div style={{ backgroundColor: color }}>
      <h1>IMG</h1>
      {personagem.nome}
    </div>
  );
};

export default CharacterButton;
